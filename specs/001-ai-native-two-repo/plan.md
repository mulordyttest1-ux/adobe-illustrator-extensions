# Implementation Plan: AI-native two-repository workstation

- Normalize the verified local product baseline into bounded commits.
- Rename and release the private devkit with WinGet, Spec Kit, Codex, doctor, backup, restore, archive, and secret guards.
- Generate the official Spec Kit Codex integration in the product and retire duplicate `.agent` instructions.
- Add an immutable sibling devkit resolver, public bootstrap prompt, product setup/doctor, compatibility commands, tests, and CI.
- Validate clean-clone independence, Windows dry-run/idempotence, devkit dirty guards, size budget, review, and verification gates.
